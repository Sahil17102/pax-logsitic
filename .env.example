# Public browser-safe backend URL for the React landing page.
VITE_API_URL=https://pax-logistic.onrender.com
# auto selects the admin app on hostnames containing "admin". Use "admin" to force it.
VITE_APP_MODE=auto
